{
  "name": "@vantage-studio/bff",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node esbuild.config.mjs",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "vsadmin:hash": "node --experimental-strip-types src/cli/hash.ts"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^8.0.3",
    "@vantage-studio/api-client": "workspace:*",
    "argon2": "^0.41.1",
    "chokidar": "^4.0.1",
    "fastify": "^5.1.0",
    "pino": "^9.5.0",
    "yaml": "^2.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
